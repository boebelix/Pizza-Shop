<?xml version="1.0" encoding="UTF-8"?>
<server description="logisticsService">

    <featureManager>
        <feature>microProfile-3.3</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="9080"
                  httpsPort="9443"/>

    <webApplication location="logisticsService.war" contextRoot="/">
        <classloader apiTypeVisibility="+third-party" />
    </webApplication>
    <mpMetrics authentication="false"/>

    <!-- This is the keystore that will be used by SSL and by JWT. -->
    <keyStore id="defaultKeyStore" location="public.jks" type="JKS" password="atbash" />


    <!-- The MP JWT configuration that injects the caller's JWT into a ResourceScoped bean for inspection. -->
    <mpJwt id="jwtUserConsumer" keyName="theKeyId" audiences="targetService" issuer="team-a"/>

	<jndiEntry id="userServicePassword" jndiName="userServicePassword" value="ferwfrwe435gsd" />
	<jndiEntry id="LogisticsLogPath" jndiName="LogisticsLogPath" value="logistics.log" />
	<jndiEntry id="UserInternURI" jndiName="UserInternURI" value="http://localhost:9080" />
</server>
